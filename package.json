{
  "name": "signalmap-pro",
  "version": "2.0.0-pro",
  "private": true,
  "workspaces": ["apps/api", "apps/web"],
  "scripts": {
    "dev:api": "npm --workspace @signalmap/api run dev",
    "dev:web": "npm --workspace @signalmap/web run dev",
    "build": "npm --workspaces run build",
    "typecheck": "npm --workspaces run typecheck",
    "migrate": "npm --workspace @signalmap/api run migrate"
  }
}
